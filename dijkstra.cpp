#include<iostream>
using namespace std;

vector<int> dijkstra(vector<vector<int>> &vec, int vertices, int egdes, int source){
    //create adjacency list
    unordered_map<int, list <pair<int, int>>> adj;
    for(int i=0; i<vertices; i++){
        int u = vec[i][0];
        int v = vec[i][1];
        int w = vec[i][2];

        adj[u].push_back
          

        
    }
}